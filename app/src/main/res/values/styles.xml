<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="editEtLine" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/lineColor</item>
        <item name="colorControlActivated">@color/lineColor</item>
        <item name="android:textCursorDrawable">@color/lineColor</item>
    </style>

</resources>
